{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://deweb.world/schemas/malt/materialization-receipt/v2/materialization-receipt.schema.json",
  "title": "MALT Materialization Receipt v2",
  "type": "object",
  "additionalProperties": false,
  "required": ["profile", "transaction_id", "base_root", "candidate", "bundle_digest", "durable_boundary"],
  "properties": {
    "profile": {"const": "malt.materialization-receipt/v2"},
    "transaction_id": {"type": "string", "pattern": "^[a-z0-9][a-z0-9._-]{0,127}$"},
    "base_root": {"type": "string", "minLength": 1, "maxLength": 4096},
    "candidate": {"type": "string", "minLength": 1, "maxLength": 4096},
    "bundle_digest": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "durable_boundary": {"type": "string", "minLength": 1}
  }
}
